query GetBooks($docid: Int){
    book(where: {docid_gt: $docid}){
        nodes{
            docid
            isbn
            genre
            doc{
                title
                pdate
            }
        }
    }
}